// Stub for optional espeak-ng / espeak-ng-emscripten packages.
// These are not installed in the dev/test environment; the real wasmTTS
// service loads them dynamically at runtime only if they exist.
export default null;
